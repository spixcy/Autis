using UnityEngine;

public class WaterZone : MonoBehaviour
{
    // This script is no longer needed since AutoUnderwaterManager handles everything.
    // The code was removed to prevent the "Tag: WaterVolume is not defined" error.
}